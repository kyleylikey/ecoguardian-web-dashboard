# API Base URL - Update with your network IP for network access
# For local development only: http://localhost:3000
# For network access: http://YOUR_NETWORK_IP:3000 (e.g., http://192.168.1.100:3000)
# Replace YOUR_NETWORK_IP with the actual IP address shown when you start the server
VITE_API_BASE_URL=http://localhost:3000

# WebSocket URL is automatically derived from VITE_API_BASE_URL (http -> ws)
# If VITE_API_BASE_URL is http://192.168.1.100:3000, WebSocket will be ws://192.168.1.100:3000